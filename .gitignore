scratch/
.DS_Store
/.beads
/.claude

# ignore pyrefly's source tree
ignore = dirty

# generated by :helptags; every plugin manager regenerates it on install
doc/tags

# design docs and captures live in scratch/; never track binaries at the root
*.pdf
*.mov
